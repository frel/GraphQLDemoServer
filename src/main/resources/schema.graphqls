# This is the main schema.

schema {
    query: Query
}

type Query {
    version: String!
    hello(what: String = "World"): String!
    item(id: ID!): Item
    allItems(first: Int, last: Int): [Item]!
}

type Item {
    id: ID!
    name: String!
    thumbnail: String!
}

